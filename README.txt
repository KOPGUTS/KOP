========================================
  opencode + DeepSeek Portable
========================================

Plug USB into any computer, no install needed.

  Linux / Mac:
    Open terminal in this folder, run:
      bash opencode.sh

  Windows:
    Double-click opencode.bat

DeepSeek API key is pre-configured.
Keep this USB safe - it contains your API key!
========================================
